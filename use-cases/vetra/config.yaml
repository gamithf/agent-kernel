whatsapp:
  agent: "vetra_triage"
  agent_acknowledgement: "🐾 Vetra is processing your request. One moment please..."

api:
  host: 0.0.0.0
  port: 8000
  max_file_size: 20971520

session:
  type: in_memory

multimodal:
  enabled: false

execution:
  mode: rest_sync

logging:
  ak:
    level: INFO
  system:
    level: WARNING
